---
title: Flutter 企业开发实践28-IM自定义消息与红包
date: 2026-08-23
tags: [Flutter, IM, 即时通讯, 自定义消息, 红包, 腾讯云 Chat, GetX, 分布式一致性, 架构, 面试]
---

# IM 自定义消息与红包

> 普通消息传递的是一段文本，红包传递的却是一笔带资金状态的交易。两者可以共用同一条 IM 通道，但不能共用同一套真值模型：消息是随时可能过期的快照，钱是只能由服务端裁决的事实。本篇是 IM 专题的终篇，讨论自定义消息协议怎么演进、红包怎么从"点一下"走到"资金入账"，以及横跨 IM 与业务服务端的一致性靠什么兜住。
>
> 前置：25 篇（IM 接入与消息收发）提供会话、消息收发与自定义消息的投递入口；26 篇（IM 登录、身份与连接治理）提供"可发送"的登录前提；27 篇（好友与群管理）提供红包的业务上下文——和谁聊、在哪个群发。

---

## 一、先定义边界：IM 是通道，不是账本

结论：IM 只负责把"有一个红包""某人领了红包"这两件事送达会话成员；红包是否存在、还剩几份、谁能领、实得多少钱，一律由业务服务端裁决。把自定义消息当成账本就等于把资金安全交给客户端。

| 层次 | 负责什么 | 不负责什么 |
|---|---|---|
| IM 服务与 IM SDK | 消息可达、会话同步、未读、离线与漫游 | 红包是否有效、份数与余额 |
| 业务服务端 | 金额、份数、领取资格、账务流水、补偿与对账 | 消息何时到达、如何渲染 |
| 客户端 | 渲染卡片快照、发起命令、展示结果 | 决定入账金额、判定领取成功 |

三条不可越过的约束：

1. 客户端上报的任何"红包状态"只是一次请求，不是事实；
2. 只有服务端命令能改变资金状态，查询与快照都只是交叉验证；
3. IM 投递失败只能影响"用户看不看得见"，不能回滚已经发生的资金事实。

资金类业务比普通消息多难在三点上：**消息是会话内共享且可写的对象**，任何成员都能改，所以"改消息"不能作为同步手段；**动作必须幂等**，弱网下一次点击会到达多次，服务端要能识别这是同一笔而不是两笔；**结果必须可追溯**，出了问题要能从用户的一次点击追到账务流水，而不是靠聊天记录反推。这三点决定了后面所有设计：协议带事件 ID、写接口带幂等键、领取靠唯一约束兜底。

**产品选择**：红包入口放在聊天页的"更多面板"；会话列表把红包类自定义消息摘要成"红包"而不是 `[自定义]`；红包卡片禁止转发，避免把一条会过期的业务指令复制到别的会话。
**通用原则**：真值在服务端、IM 只做投递；至少一次投递配幂等消费；唯一约束兜底。这三条与业务形态无关，任何"有状态业务 + 消息通道"的组合都适用。

## 二、自定义消息协议与演进规则

结论：自定义消息的正文本身就是协议，必须自带类型、版本与事件 ID，否则第一次字段变更就要靠客户端猜。

自定义消息的 data 通常是一段 JSON 字符串，建议统一收敛到下面这样的信封（示意结构，字段值均为通用占位）：

```json
{
  "schema": "chat.business_event",
  "version": 2,
  "eventId": "全局唯一事件 ID",
  "type": "redpacket.created",
  "occurredAt": "2026-01-01T10:00:00Z",
  "conversation": { "type": "group", "id": "会话 ID" },
  "actor": { "id": "业务用户 ID", "displayName": "昵称", "avatarUrl": "头像地址" },
  "aggregate": { "type": "redpacket", "id": "业务实体 ID", "revision": 1 },
  "payload": { "remark": "恭喜发财", "cover": "default", "packetType": "random" }
}
```

| 字段 | 语义 | 设计理由 |
|---|---|---|
| schema + version | 协议名与协议版本 | 解析器入口只认这两个字段，未知版本走降级 |
| eventId | 全局唯一事件标识 | 客户端去重、投递重试排查、全链路追踪 |
| type | 过去式事件名 | 表达"服务端已经发生了这件事"，不是"请执行" |
| aggregate.id | 业务实体 ID | 消息可以重发，业务实体不能被重复创建 |
| aggregate.revision | 实体状态版本 | 客户端只接受更新版本，天然抗乱序与重复 |
| payload | 渲染所需快照 | 不放支付密码、账务明细与完整身份信息 |

三条演进规则：

- **业务 ID 与版本是同一个字段的两半**。业务 ID 回答"这是哪笔交易"，版本回答"这是它的第几个状态"。缺少版本的协议在多人并发场景下无法判断新旧，只能靠到达顺序，而到达顺序在 IM 通道上毫无保证。
- **事件化命名**：`redpacket.created`、`redpacket.claimed`、`redpacket.expired`。命令式命名（如"领取红包"）会诱导接收方把消息当指令执行，一旦消息被转发或重放就会造成重复动作。
- **未知类型必须降级**：解析失败或不认识版本时，渲染成"暂不支持的消息"占位符，保留原始 JSON 供排查，绝不让一条脏数据打崩整个消息列表。消息列表里天然混合着不同时期、不同版本、不同客户端写进去的数据，这是通用要求而非个别场景的补丁。

还有一条容易被忽略的规则：**类型名用字符串，不用数字枚举**。数字枚举看着紧凑，但它的含义完全依赖两端对约定版本的共同理解，一旦某一边把同一个数字理解成别的业务，解析器不会报错，只会表现为"红包卡片渲染成了别的东西"。字符串类型名至少能在日志与排查工具里自解释，也便于按类型分别统计解析失败率。

解析结果建议收敛成密封类型，渲染层只做穷举分支，而不是到处判断字符串：

```dart
sealed class ChatEvent {
  const ChatEvent();
}

final class RedPacketCreated extends ChatEvent {
  const RedPacketCreated({required this.packetId, required this.revision});
  final String packetId;
  final int revision;
}

final class RedPacketClaimed extends ChatEvent {
  const RedPacketClaimed({
    required this.packetId,
    required this.revision,
    required this.receiverId,
  });
  final String packetId;
  final int revision;
  final String receiverId;
}

final class UnsupportedChatEvent extends ChatEvent {
  const UnsupportedChatEvent(this.rawType);
  final String? rawType;
}
```

迁移分四阶段，不能一次切换：

1. 客户端先上线双协议解析器，此时只读旧格式；
2. 服务端双写：新信封 + 旧客户端的必要字段，新旧客户端都能渲染同一条消息；
3. 按版本活跃率与解析失败率确认旧客户端占比低于业务阈值；
4. 服务端停止写兼容字段，但客户端**永久保留**旧格式解析能力，历史消息仍要能看。

## 三、发红包：配置、支付、创建业务实体、投递卡片

结论：顺序必须是"读配置 → 校验 → 支付 → 服务端创建业务实体 → 投递卡片"，反过来就会产生一条用户看得见却领不到的幽灵红包。

可发金额与可用份数来自服务端下发的配置，而不是写死在客户端：功能开关、单聊与群聊的可用性、份数范围、单笔固定上限、基于用户可用额度的百分比上限、支付密码与封面约束。最终可发金额取"固定上限"和"额度百分比上限"的较小者。客户端校验只为即时反馈，服务端必须把全部规则重跑一遍。

配置与支付两步的失败要分开向用户说明：额度不足或超出限额属于业务拒绝，应直接告诉用户当前可发多少，而不是让他反复试；支付取消是用户的主动行为，不能报成错误，回到输入页并保留已填内容即可；服务端创建失败则必须明确"红包未发出、资金未冻结"，避免用户以为钱已经扣了。话术不准确，会把交互问题升级成资金投诉。

金额一律用"分"的整数运算，不要用浮点数表示钱。拆分算法（二倍均值法）是最常见的答案：

```text
// 示意伪代码：二倍均值法拆分，金额单位为"分"
// 演示用途，未做参数校验
split(totalCents, count):
    remain      = totalCents
    remainCount = count
    result      = []
    for i in 1 .. count-1:
        upper = remain / remainCount * 2          // 均值的两倍为上界
        piece = randomInt(1, upper)
        piece = min(piece, remain - (remainCount - 1))   // 给后面每人留 1 分
        result.append(piece)
        remain      -= piece
        remainCount -= 1
    result.append(remain)                          // 最后一份拿剩余全部
    return result
```

比算法更重要的两个决策：

1. **预生成还是领取时计算**。预生成（发红包时一次拆好入库）直观，但过期退款要退回未领份；领取时在事务内实时计算没有预存状态，天然支持动态调整。两者的并发控制与对账口径完全不同，选型时先定这一条。
2. **拆分必须发生在服务端事务内**。客户端算好金额上传，等于把分钱权交给攻击者。

发送时序：

```text
用户        客户端            业务服务端                投递器        IM 服务
 │  填金额份数 │                    │                      │              │
 │────────────>│                    │                      │              │
 │  支付密码   │───创建(幂等键)────>│                      │              │
 │             │                    │ 校验限额/额度         │              │
 │             │                    │ 冻结资金+落库实体      │              │
 │             │                    │ 写待投递事件           │              │
 │             │<──实体 ID + 状态───│                      │              │
 │             │                    │───待投递事件─────────>│              │
 │             │                    │                      │──投递事件───>│
 │             │                    │                      │              │──> 会话成员
```

这个顺序不能反。先发卡片再创建实体，会话里就会多出一条永远领不到的消息：用户点不开、无法退款、客服也解释不清。而"第 4 步成功、第 5 步失败"同样危险：资金已冻结，会话里却没有卡片，用户根本找不到入口。这类缺口不能靠客户端 try-catch 弥补，服务端至少要提供一种能力：带幂等键的重发、短时间内可撤销未领取红包，或用待投递事件表交给可重试任务投递（第七节）。

## 四、领红包：快照检查、服务端原子领取、定向回执

结论：可用性检查只是为了少让用户白点一次，真正建立资金事实的是服务端那条原子命令。

```text
用户        客户端                  业务服务端            IM 服务
 │ 点击卡片    │                        │                    │
 │────────────>│ 读本地快照(已领完/过期→详情)               │
 │             │──可用性检查(我是否已领/可否领)─>│            │
 │             │<────────可领取───────────│            │
 │             │ 播放开红包动画            │            │
 │             │──领取(幂等键+命令 ID)────>│            │
 │             │                        │ 事务内校验并原子入账 │
 │             │<──本次金额+最新状态───────│            │
 │             │ 用返回值更新卡片投影       │            │
 │             │──领取回执(定向)───────────────────────>│
 │  进入详情页  │                        │                    │
```

检查与执行之间存在并发窗口：两个用户可以同时通过可用性检查。所以服务端在领取事务里必须重新校验状态、份数与过期时间，并用条件更新、行锁或唯一约束保证不超发。提前检查的价值是交互体验，不是安全边界。

开红包动画这里还有一个容易做错的细节：动画只是等待期的过渡表现，不能当成功信号。领取请求返回之前，界面保持"处理中"并禁用重复点击；返回之后，由服务端结果决定是展示金额还是解释失败原因。若动画播完即视为成功，弱网下会出现"看到金额但服务端并未入账"的错觉，这种错觉在资金场景里代价极高。

回执要按会话类型分流：

| 场景 | 回执目标 | 原因 |
|---|---|---|
| 单聊红包 | 红包发送者 | 单聊只有两个人，直接发给对方 |
| 群红包 | 仅红包发送者 | 每次领取都广播会在群里刷屏 |
| 只有本机可见的状态提示 | 本地插入消息（只写本地、不投递），或只在本机渲染 | 不能用"发送后再删除"：删除是本地语义，对方在投递成功时已经可见，批量删除也不支持多端同步 |

群定向消息的语义、可用性与套餐能力由 IM 服务商定义，不是所有会话类型都支持；在单聊里创建定向消息是接口语义错误，正确的做法是群聊走定向、单聊直发。

最容易被写错的一处是失败归因：**服务端领取已经成功、后续 IM 操作失败时，不能对用户说"领取失败"**。资金事实已经建立，回执与卡片更新只是通知同步。正确做法是记录可观测日志、照常进入详情页，让用户看到服务端最终状态。把通知失败伪装成资金失败，会诱发重复请求、重复客服沟通，甚至引发"钱被吞了"的判断。

| 操作 | 性质 | 失败后 |
|---|---|---|
| 可用性检查 | 只读查询 | 提示稍后重试，不改变任何状态 |
| 服务端领取 | 资金主交易 | 按错误码向用户说明，不确定就查详情 |
| 更新本地卡片投影 | UI 同步 | 记日志，由详情页或快照接口纠正 |
| 发送领取回执 | 通知同步 | 可重试、最终一致，不回滚资金事实 |

## 五、三层一致性模型：卡片快照、服务端查询、服务端命令

结论：客户端同时握着三份状态，优先级由"谁有权改变资金事实"决定。

```text
第三层  服务端原子命令   创建 / 领取      唯一能改变资金状态，最终真值
              ↑ 校验
第二层  服务端查询       可用性 / 快照    交叉验证，结果随时可能被并发改变
              ↑ 兜底
第一层  卡片快照         消息体里的状态   立即可渲染，随时可能过期
```

| 层次 | 维护者 | 允许用来做什么 | 绝不允许用来做什么 |
|---|---|---|---|
| 卡片快照 | 消息体，投递时写入 | 列表渲染、状态角标、快速拦截明显失效 | 判定能否领取、判定已领金额 |
| 服务端查询 | 业务服务端 | 弹窗前的可用性确认、详情页真相、重连纠正 | 替代领取事务 |
| 服务端命令 | 业务服务端 | 建立资金事实、返回最终结果 | —— |

由此得到两条落地规则：

- 列表页可以只依赖快照追求流畅，但用户一点击、一看详情、一涉及金额，就必须回到服务端；
- 事件乱序时用版本号做单调合并：只接受 revision 更大的事件，发现版本跳号就拉一次快照纠正，而不是等下一个事件到达。

## 六、为什么不能让客户端改消息来维护领取名单

结论：一旦把"谁领过"写进消息体并由客户端维护，领取名单就变成了一个没有事务、没有唯一约束、没有权限控制的共享变量。

### 6.1 用字符串子串判重

- **可观察现象**：部分用户点开自己从未领过的红包，界面却提示"你已领取"，随后被拦住无法继续。
- **业务后果**：用户认为钱被吞了，客服工单上升，同时"已领名单"与资金明细对不上，对账口径被污染。
- **通用成因**：领取人列表以逗号分隔的字符串保存，判重用的是子串包含。用户 ID 互为子串时（短 ID 是长 ID 的一部分）就会命中假阳性。
- **可迁移对策**：先解析成精确集合再判断；更彻底的做法是把名单移出消息体，改由服务端查询。

```dart
Set<String> parseUserIds(String? raw) => (raw ?? '')
    .split(',')
    .map((id) => id.trim())
    .where((id) => id.isNotEmpty)
    .toSet();

bool hasPicked(String? raw, String userId) =>
    userId.isNotEmpty && parseUserIds(raw).contains(userId);
```

精确匹配只是兼容存量协议的兜底修正，它没有解决更根本的问题：名单为什么会在客户端。

### 6.2 多人并发改同一条消息

- **可观察现象**：群红包被多人同时领取后，卡片上的已领人数偶尔比实际少，或者刚显示 5 人又退回 3 人；不同成员手机上看到的份数长期不一致。
- **业务后果**：用户对"还剩几份"判断错误，出现"看着有、点进去没了"的体验崩塌；服务端明细与消息体永久分叉，任何排查都得从日志重新拼事实。
- **通用成因**：客户端"读消息 → 改消息 → 上传"是典型的读-改-写循环，中间没有任何版本检测。N 个客户端并发操作同一条消息时，最后一次写入覆盖其余所有人；而消息在 IM 服务上是一个全局共享对象，会话内每个成员都能修改它。
- **可迁移对策**：客户端改消息只允许用于本地渲染的即时反馈，不承担真值；跨端同步改用带版本的投影事件；用户进入详情、重连或发现版本跳号时，用快照接口纠正本地投影。

版本化投影的最小形态是这样的：

```dart
enum RedPacketStatus { preparing, active, exhausted, expired, cancelled }

final class RedPacketProjection {
  const RedPacketProjection({
    required this.packetId,
    required this.status,
    required this.claimedCount,
    required this.totalCount,
    required this.revision,
    required this.claimedByMe,
  });

  final String packetId;
  final RedPacketStatus status;
  final int claimedCount;
  final int totalCount;
  final int revision;
  final bool claimedByMe;

  /// 只接受更新的版本，重复与乱序事件都会被自然丢弃。
  RedPacketProjection merge(RedPacketProjection incoming) =>
      incoming.revision > revision ? incoming : this;
}
```

投影只保留粗粒度状态：状态、已领份数、总份数、版本、我是否已领。它不保存领取人全量列表，也不参与任何资金校验。若确实需要让原消息卡片在其他端更快看到"已领完"，可以评估 IM 服务商提供的消息扩展能力，只写入有上限的粗粒度字段；上线前必须按实际 SDK 版本与套餐验证支持范围，扩展值仍然是 UI 快照，不是账本。

判断一个字段能不能放进消息体，只需要问一句：**如果它在所有客户端上的值都不一致，会不会影响到钱**。会，它就只能存在于服务端，消息里最多放一个用于渲染的近似值；不会（例如备注、封面、昵称快照），放在消息里反而是合理优化，因为它省掉一次网络请求，并且天然随会话分发。

## 七、可靠性架构：Outbox、幂等与并发领取

结论：跨系统的一致性靠四件事组合——事务内写业务实体与待投递事件、幂等键、唯一约束兜底、至少一次投递配幂等消费。任何一次请求的成败都不足以为一致性背书。

### 7.1 Outbox：资金已动，卡片未到

- **可观察现象**：偶发"钱被冻了但聊天里没有红包"，用户反复下拉会话列表也等不到卡片，重进页面依旧没有。
- **业务后果**：资金被冻结却找不到入口，只能人工退款；用户无法理解发生了什么，客服也没有可查的证据链。
- **通用成因**：创建红包与"通知 IM"是两次独立的外部调用，中间任何一次崩溃、断网或进程被杀都会留下不一致。在分布式环境里"两次远程写都成功"本来就是无法保证的。
- **可迁移对策**：Transactional Outbox。创建业务实体与写入待投递事件放在同一个数据库事务内，事务只做本地写入，**绝不在事务内调用外部 IM 接口**，投递交给独立任务重试。

```sql
-- 示意 DDL：红包实体与待投递事件
CREATE TABLE redpacket (
  id              VARCHAR(64) PRIMARY KEY,
  sender_id       VARCHAR(64) NOT NULL,
  conversation_id VARCHAR(128) NOT NULL,
  status          VARCHAR(32) NOT NULL,
  total_cents     BIGINT NOT NULL,
  claimed_cents   BIGINT NOT NULL DEFAULT 0,
  total_count     INT NOT NULL,
  claimed_count   INT NOT NULL DEFAULT 0,
  revision        BIGINT NOT NULL DEFAULT 1,
  expires_at      TIMESTAMP NOT NULL,
  idempotency_key VARCHAR(64) NOT NULL,
  UNIQUE (sender_id, idempotency_key)
);

CREATE TABLE outbox_event (
  event_id      VARCHAR(64) PRIMARY KEY,
  aggregate_id  VARCHAR(64) NOT NULL,
  event_type    VARCHAR(64) NOT NULL,
  payload       JSON NOT NULL,
  status        VARCHAR(16) NOT NULL,
  retry_count   INT NOT NULL DEFAULT 0,
  next_retry_at TIMESTAMP NULL,
  created_at    TIMESTAMP NOT NULL,
  delivered_at  TIMESTAMP NULL,
  im_message_id VARCHAR(128) NULL
);
```

```text
-- 示意伪代码：创建红包的本地事务
BEGIN
  校验幂等键 —— 命中则直接返回首次结果
  校验发送人资格、限额、份数、可用额度
  冻结资金并写冻结流水
  INSERT redpacket   (status = PENDING_DISPATCH)
  INSERT outbox_event(status = PENDING, type = redpacket.created)
COMMIT
```

投递器通过行锁或等价的队列抢占方式取任务，状态机很窄：

```text
PENDING ──抢占（带租约）──→ SENDING
SENDING ──IM 投递成功──→ DELIVERED（红包转 ACTIVE）
SENDING ──可重试失败──→ PENDING（指数退避，retry_count + 1）
SENDING ──超过最大次数──→ DEAD（进入补偿队列）
SENDING ──租约超时未被回收──→ PENDING（崩溃恢复出口）
```

`SENDING` 必须有一个崩溃恢复出口，否则它自己就变成了新的"中间态陷阱"：投递器抢占之后进程崩溃或被重启，任务会永久滞留在这个状态——既不重试也不进死信，红包一直停在"准备中"，资金冻结到天荒地老。做法是抢占时写入带过期时间的租约（`lease_until`），由看门狗把"租约已过期但仍在 `SENDING`"的任务回收为 `PENDING`（或超过重试上限直接判 `DEAD`）。

三个细节不能省：租约要由投递方在投递期间**续期**，否则长耗时投递会被误判成崩溃、引发重复投递；回收动作必须**幂等**，避免看门狗与正常投递同时改同一条任务；租约回收与重试上限要共用同一个计数器，否则每次回收都把计数清零，死信永远进不去。这也解释了为什么这类系统必须有基于时间的兜底：跨系统投递没有事务可用，任何中间状态都必须能被时间捞回来。

红包在 `PENDING_DISPATCH` 期间只冻结资金，不允许领取。若卡片先到、状态还没切换，可用性检查返回"准备中"，客户端显示"红包准备中"并做短轮询，而不是直接报错。

死信补偿只有三种处置：仍未投递且没有任何领取记录，取消红包并解冻全部金额；有投递证据但状态回写失败，修正为 ACTIVE 且不退款；无法自动判定，进人工对账队列，**绝不让客户端决定资金结果**。

### 7.2 IM 已投递但业务状态未回写

- **可观察现象**：红包卡片在会话里正常显示，但主表状态仍是待投递；用户点击后一会儿能领、一会儿报"红包不存在"，不同入口表现不一致。
- **业务后果**：同一笔红包在不同入口给出矛盾答案，用户反复重试；若投递任务又把状态回滚，还会出现"已领过的红包被重新放回可领"这种更严重的问题。
- **通用成因**：投递器在"IM 已成功、本地尚未标记成功"的窗口里崩溃或超时，重启后无法区分"没发出去"和"发出去了但没记住"。这是跨系统的典型歧义，不是偶发 bug。
- **可迁移对策**：把事件 ID 作为幂等标识，投递结果允许重复到达；接收端按事件 ID 去重，重复事件只刷新投影、不重复计数、不重复渲染；投递器对同一事件的重发必须携带相同的事件 ID 与版本，且永远不做"状态回退"的写入。

### 7.3 领取请求超时后的重复领取风险

- **可观察现象**：弱网下用户点了一次领取，界面转圈后报超时；用户再点一次，结果出现两条领取记录，或提示"你已领取"，金额却与第一次不同。
- **业务后果**：同一人拿到两笔钱，红包份额被提前消耗，其余成员少领；对账时主表已领金额与明细合计不一致，只能人工冲正。
- **通用成因**：网络超时不代表服务端执行失败。用户重复点击、客户端自动重试、App 重启后恢复请求都会重发同一个动作，而服务端把它当成了新请求。
- **可迁移对策**：所有写接口都接受客户端生成的幂等键，服务端保存"用户 + 操作 + 幂等键"与首次响应。同键同参数重复请求直接返回首次结果；同键不同参数返回冲突错误，避免一个键被复用到两笔不同交易上。接口路径是通用示意：

```text
POST /api/red-packets
Idempotency-Key: <客户端生成，全局唯一>

POST /api/red-packets/{packetId}/claims
Idempotency-Key: <客户端生成，全局唯一>
```

### 7.4 唯一约束是最后一道保护

```sql
-- 示意 DDL：领取明细与两条唯一约束
CREATE TABLE redpacket_claim (
  id              VARCHAR(64) PRIMARY KEY,
  packet_id       VARCHAR(64) NOT NULL,
  receiver_id     VARCHAR(64) NOT NULL,
  amount_cents    BIGINT NOT NULL,
  idempotency_key VARCHAR(64) NOT NULL,
  created_at      TIMESTAMP NOT NULL,
  UNIQUE (packet_id, receiver_id),
  UNIQUE (receiver_id, idempotency_key)
);
```

```text
-- 示意伪代码：领取红包的原子事务
BEGIN
  1. 查幂等记录，已有结果直接返回
  2. 锁定红包行，校验 ACTIVE、未过期、剩余份数 > 0
  3. 事务内计算本次金额（按"分"做整数运算）
  4. INSERT 领取明细 —— 由唯一约束挡住同一人重复领取
  5. 累加已领份数与已领金额，revision + 1
  6. 最后一份被领走时状态改为 EXHAUSTED
  7. 写账务流水与 redpacket.claimed 待投递事件
COMMIT
```

行锁、条件更新、乐观版本号都可以用，但唯一约束必须存在：它是"并发"和"重试"两种风险同时出现时唯一不会失效的那一层。只靠缓存里的分布式锁，一旦锁过期或缓存抖动，超发就会直接从数据库层落下来。

### 7.5 至少一次投递 + 幂等消费

投递器可能在"IM 已成功、本地未标记成功"时崩溃，重启后重发同一个事件 ID。跨系统严格 exactly-once 很难实现，务实的目标是把投递语义收敛为"至少一次投递 + 幂等消费"：客户端按事件 ID 去重，服务端按事件 ID 记账。这条原则同时解释了为什么事件 ID 必须出现在协议信封里——它不是为了好看，而是重试与去重的唯一依据。

## 八、SDK 隔离、可观测性与对账

结论：页面不应该直接和 IM SDK 说话，也不应该自己判断资金结果；可观测性与对账是把"猜"变成"查"的基础设施。

把边界收敛成两个接口：一个负责收发自定义消息，一个负责资金相关的 HTTP 命令与快照。

```dart
abstract interface class ChatGateway {
  Stream<ChatEvent> get events;

  Future<void> sendBusinessEvent({
    required ChatEvent event,
    Set<String> visibleTo = const {},
  });
}

abstract interface class RedPacketRepository {
  Future<void> create({required int totalCents, required int count});

  Future<void> claim({
    required String packetId,
    required String idempotencyKey,
  });

  Future<RedPacketProjection> getSnapshot(String packetId);
}
```

- 消息适配层吸收单聊与群聊的投递差异、定向消息的可用性差异，以及"创建消息"这类两步返回（错误码与数据都要判空，不能强制解包）；
- 仓储层吸收 HTTP 错误码与幂等响应；用例层只组织业务流程，返回值由服务端结果决定，IM 回执是否成功不影响它；
- 收益是可测性：超时、重试、事件重复、事件乱序都能用假实现覆盖，不必真的登录 IM 才能验证。

这种划分还有一个附带收益：IM SDK 升级只影响消息适配层的一个实现。页面直接调 SDK 的写法，一次升级要在多个页面分别回归；收敛成接口之后，"哪些行为依赖 SDK 的返回结构"变成一份可枚举的清单，升级前按清单做定向验证即可。

```text
红包主表冻结/已领金额
        ↕
领取明细金额合计
        ↕
账务流水的冻结、入账、退回
        ↕
待投递事件的投递与死信状态
```

对账规则至少覆盖：已领金额等于领取明细合计；发送者冻结金额等于已领 + 待领 + 已退；`EXHAUSTED` 的已领份数等于总份数；`EXPIRED`/`CANCELLED` 红包的未领部分已生成退回流水；`ACTIVE` 红包必须有一条成功投递的创建事件；已投递事件保留 IM 消息 ID 以便排查。发现差异时先冻结异常实体的后续操作，再执行可重复的补偿命令；补偿命令同样要带幂等键，否则任务重启会重复退款。

可观测性：命令 ID、事件 ID、红包 ID、IM 消息 ID 要贯穿客户端日志、HTTP 请求、数据库记录与投递日志。不记录支付密码、登录凭证、完整身份信息与不必要的金额明细。脱敏要落到具体字段上，实现上建议把"可记录字段"做成白名单，而不是逐处判断"这个字段敏感吗"——逐处判断在字段增删时必漏。

| 指标 | 用途 |
|---|---|
| 创建量 | 业务基线与异常波动 |
| 领取冲突数 | 最后一份竞争与重复领取情况 |
| 投递延迟 | 创建成功到卡片可见的时间 |
| 投递重试数 | 对 IM 依赖的短期稳定性 |
| 死信数 | 需要补偿或人工介入的事件量 |
| 事件解析失败数 | 协议兼容与脏数据问题 |
| 投影版本跳号数 | 事件丢失、乱序与快照纠正频率 |

## 九、常见错误与修正表

结论：下面的问题在"有状态的业务 + 消息通道"的系统里会反复出现，先看业务后果，再决定对策的轻重。

| 问题 | 业务后果 | 对策 |
|---|---|---|
| 用字符串子串判断"我是否已领" | 用户被误判为已领，无法领取 | 解析为精确集合作判重；名单移出消息体 |
| 客户端读-改-写消息维护领取名单 | 并发覆盖，多端状态分叉 | 真值放服务端；客户端只维护版本化投影 |
| 先投递卡片再创建业务实体 | 会话里出现永远领不到的幽灵红包 | 顺序固定为"创建实体 → 投递卡片" |
| 领取请求超时后无幂等键直接重试 | 同一人重复领取，份额被提前消耗 | 写接口强制幂等键 + 唯一约束 |
| 领取事务只靠分布式锁，无唯一约束 | 锁失效时超发，主表与明细不一致 | 唯一约束作为最后一道保护 |
| 服务端领取成功、IM 回执失败时提示"领取失败" | 用户重复请求，认为钱被吞 | 回执失败只记日志，继续展示服务端事实 |
| 在单聊里创建定向群消息 | 回执发送失败，语义错误 | 群聊定向、单聊直发，按会话类型分流 |
| 自定义消息不带版本与事件 ID | 字段一改旧客户端解析崩溃、无法去重 | 版本化信封 + 事件 ID + 未知类型降级 |
| 未知类型消息直接抛异常 | 单条脏数据打崩整个消息列表 | 降级为"暂不支持的消息"占位 |
| 金额使用浮点数计算 | 拆分与合计出现分位误差 | 一律用"分"的整数运算 |
| 过期红包不退款、死信不补偿 | 资金被长期冻结，无法回收 | 到期退款与死信补偿形成闭环 |
| 创建消息结果强制解包 | 创建失败时崩溃 | 错误码与数据两层都判空 |
| 投递中间态没有超时回收 | 任务永久停在投递中，红包长期"准备中"、资金一直冻结 | 抢占时写租约，看门狗回收过期任务，且回收幂等 |

## 十、验收清单

结论：验收要按失败路径设计，而不是把主流程点通就算完。

| 场景 | 验收标准 |
|---|---|
| 发红包 | 资金冻结、实体落库、卡片投递三者最终一致；金额与份数由服务端校验 |
| 重复点击发送 | 同一幂等键多次请求只产生一个红包与一笔冻结流水 |
| 并发领取最后一份 | 只有一个请求成功，主表、明细与账务金额完全一致 |
| 同一用户重复领取 | 被唯一约束拒绝，返回首次结果而非第二笔金额 |
| 领取请求超时重试 | 重试返回首次结果，不新增明细、不重复入账 |
| IM 投递失败 | 待投递事件可重试；恢复后卡片最终可见，且不重复渲染同一事件 |
| 投递达到死信阈值 | 未投递且无领取记录的红包被取消并解冻，不留不可访问的活跃资金 |
| 投递器抢占后进程崩溃 | 租约到期后任务被回收重投，不会永久停在"投递中"，红包也不会长期停在"准备中" |
| 跨版本消息解析 | 新旧协议都能渲染同一条消息；未知类型降级为占位，列表不崩 |
| 旧客户端兼容 | 迁移期内旧客户端仍可显示与领取；历史消息始终可读 |
| 红包过期与退款 | 到期后未领部分生成退回流水，卡片显示为已过期且不可再领 |
| 事件乱序与重复 | 版本更小的事件被丢弃；版本跳号触发一次快照纠正 |
| 对账 | 任意异常交易可通过红包 ID 追溯到命令 ID、事件 ID、账务流水与 IM 消息 ID |

## 十一、面试问答

结论：这些问题最终都指向同一条边界——谁有权改变事实。

**Q1：为什么红包不能只用 IM 自定义消息实现？**
因为 IM 解决的是投递，不是交易。自定义消息的正文由客户端构造、由客户端修改、在会话内共享可写，天然没有原子性、没有唯一约束、没有权限边界。资金要求"唯一、原子、可追溯"，这三条只能在服务端事务里满足。由此也能推出客户端改消息的唯一合法用途：本机渲染的即时反馈。判据不是"能不能改"，而是"改了之后错了会不会影响钱"。

**Q2：既然有可用性检查，为什么还要服务端重新校验？**
检查与执行之间存在时间窗口，两个用户可以同时通过检查。可用性检查的作用是让用户在明显不可领时少点一次，避免无谓的开红包动画；真正的约束必须在领取事务里重新施加，否则并发场景下份数必然超发。

**Q3：IM 回执失败，能不能告诉用户"领取失败"？**
不能。资金事实在服务端领取返回成功的那一刻就建立了，回执只是通知同步。把通知失败当成资金失败，用户会重复领取、重复联系客服，还会怀疑资金安全。正确做法是记录可观测日志并继续展示服务端最终状态。

**Q4：为什么群领取回执要做成定向消息？**
回执的受众只有红包发送者。若每次领取都向全群投递一条消息，一个 200 人的群红包会产生 200 条噪声消息，把正常聊天完全淹没。定向消息既保证发送者能收到，也不打扰其他人；同时要注意定向能力受会话类型与套餐约束，单聊场景应直发。

**Q5：自定义消息协议为什么必须带版本和事件 ID？**
版本解决"怎么变"：没有版本就只能靠字段是否存在做兼容判断，第一次改名就会让旧客户端解析失败。事件 ID 解决"是否处理过"：网络重试、投递重试、用户重复点击都会让同一条事件到达多次，没有事件 ID 就无法去重，也无法在排查时把客户端日志、服务端事件与 IM 消息串成一条链路。

**Q6：为什么说"至少一次投递 + 幂等消费"比追求 exactly-once 更现实？**
因为投递器可能在"IM 已成功、本地未标记成功"的窗口里崩溃，跨系统无法原子地同时提交两边的状态。与其引入代价高昂的分布式事务，不如承认重复是常态：投递端允许重发同一事件 ID，接收端按事件 ID 去重，业务结果依然唯一。

## 参考资料

- [Tencent Cloud Chat：Flutter SDK API 概览](https://trtc.io/document/40124?menulabel=core+sdk&platform=flutter&product=chat)
- [Tencent Cloud Chat：自定义消息](https://trtc.io/document/47993)
- [Tencent Cloud Chat：修改消息](https://trtc.io/document/48004)
- [Tencent Cloud Chat：定向群消息](https://trtc.io/document/48028?product=chat)
- [Tencent Cloud Chat：消息扩展](https://trtc.io/document/52489?menulabel=core+sdk&platform=flutter&product=chat)
- [microservices.io：Pattern: Transactional outbox](https://microservices.io/patterns/data/transactional-outbox.html)
- [microservices.io：Pattern: Idempotent Consumer](https://microservices.io/patterns/communication-style/idempotent-consumer.html)
- [AWS Prescriptive Guidance：Transactional outbox pattern](https://docs.aws.amazon.com/prescriptive-guidance/latest/cloud-design-patterns/transactional-outbox.html)
- [RFC 9110：HTTP Semantics — Idempotent Methods](https://www.rfc-editor.org/rfc/rfc9110#name-idempotent-methods)
